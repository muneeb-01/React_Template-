const NoLayoutPage = () => (
  <div className="min-h-screen bg-gradient-to-r from-purple-400 to-blue-500 flex items-center justify-center">
    <div className="text-white text-center">
      <h2 className="text-3xl font-bold mb-4">No Layout Page</h2>
      <p>This page does not use the default layout.</p>
    </div>
  </div>
);

export default NoLayoutPage;
